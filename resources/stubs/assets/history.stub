import {Page} from 'laravel-jstools';

export default class $MODEL_FULL_PATH_UNDERSCORE$_History extends Page {
    init() {
        window.tableHistory(this.getDefaultTableHistoryId('$MODEL$'));
    }
}
